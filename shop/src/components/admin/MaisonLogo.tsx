import React from "react";

/**
 * Login-screen brand mark — replaces the default Payload logo.
 * Styled in src/app/(payload)/custom.scss (.miu-brand).
 */
export function MaisonLogo() {
  return (
    <span className="miu-brand miu-brand--logo" translate="no">
      Maison IU
    </span>
  );
}
